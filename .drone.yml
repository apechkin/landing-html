---
kind: pipeline
name: constructor
steps:
  - name: landing-html
    image: mhart/alpine-node:10
    when:
      branch:
        - develop
    commands:
      - npm install -g npm
      - npm install
  - name: ssh
    image: appleboy/drone-ssh
    when:
      branch:
        - develop
    host: pechkin.space
    username: $ssh_username
    password: $ssh_password
    port: 22
    script:
      - docker stop landing-html
      - docker rm -f landing-html
      - docker run --restart always --name landing-html \
      - -e VIRTUAL_HOST=testpage.pechkin.space \
      - -e LETSENCRYPT_HOST=testpage.pechkin.space \
      - -e LETSENCRYPT_EMAIL=anatmozg@mail.ru \
      - -d landing-html
      - docker rmi $(docker images -f "dangling=true" -q)
---
kind: signature
hmac: ca3a347a753a98b89476f369e68f1337b9b661ad3f01cce4a50a9907d42419e9

...
